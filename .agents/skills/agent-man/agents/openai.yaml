interface:
  display_name: "Agent Man"
  short_description: "Safely sync native agent config and encrypted secrets"
  default_prompt: "Use $agent-man to inspect, plan, and safely synchronize my native agent configuration and any explicitly opted-in age-encrypted secrets."
